#include "ColorRGB.h"

namespace TinyGraphics {

  const ColorRGB ColorRGB::BLACK  (0, 0, 0);
  const ColorRGB ColorRGB::WHITE  (1, 1, 1);

  const ColorRGB ColorRGB::RED    (1, 0, 0);
  const ColorRGB ColorRGB::YELLOW (1, 1, 0);
  const ColorRGB ColorRGB::GREEN  (0, 1, 0);
  const ColorRGB ColorRGB::CYAN   (0, 1, 1);
  const ColorRGB ColorRGB::BLUE   (0, 0, 1);
  const ColorRGB ColorRGB::MAGENTA(1, 0, 1);
}
